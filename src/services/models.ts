export interface IAddUserModel {
    FirstName: string;
    LastName: string;
    Email: string;
}

export interface IGetUsersResponseModel {
    id: number;
    FirstName: string;
    LastName: string;
    Email: string;
}